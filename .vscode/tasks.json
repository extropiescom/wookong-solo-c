{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "make",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}/dev/"
            }
        }
    ],
    "version": "2.0.0"
}